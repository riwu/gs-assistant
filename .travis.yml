language: node_js
node_js:
  - 8
cache:
  directories:
  - node_modules
notifications:
  email: false
deploy:
  provider: pages
  skip-cleanup: true
  github-token: $GITHUB_TOKEN
  local-dir: build
  on:
    branch: master
